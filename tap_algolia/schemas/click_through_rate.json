{
  "type": "object",
  "properties": {
    "index_name": {
      "type": "string",
      "description": "The Algolia index name"
    },
    "clickCount": {
      "type": ["integer", "null"],
      "description": "Number of clicks"
    },
    "trackedSearchCount": {
      "type": ["integer", "null"],
      "description": "Number of tracked searches"
    },
    "rate": {
      "type": ["number", "null"],
      "description": "Click-through rate (0.0-1.0)"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Date of the metric"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Start date of the data window"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "End date of the data window"
    }
  },
  "required": ["index_name", "date"]
}