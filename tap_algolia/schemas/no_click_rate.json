{
  "type": "object",
  "properties": {
    "index_name": {
      "type": "string",
      "description": "The Algolia index name"
    },
    "count": {
      "type": ["integer", "null"],
      "description": "Total number of tracked searches"
    },
    "noClickCount": {
      "type": ["integer", "null"],
      "description": "Number of searches without clicks"
    },
    "rate": {
      "type": ["number", "null"],
      "description": "No-click rate (0.0-1.0)"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Date of the metric"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Start date of the data window"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "End date of the data window"
    }
  },
  "required": ["index_name", "date"]
}